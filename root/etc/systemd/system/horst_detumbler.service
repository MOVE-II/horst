[Unit]
Description=HORST: DEPLOYED Signal
After=horst.service

[Service]
Type=oneshot
#User=misc
ExecStart=/bin/dbus-send --dest=moveii.horst --type=signal /moveii/horst moveii.horst.leopStateChange string:"DEPLOYED"

[Install]
WantedBy=skip-LEOP.target
